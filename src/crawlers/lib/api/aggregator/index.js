



/*
Parser (which is applied in worker) returns an object with data.

 */

function getAggregator() {
    return core => next => initWorker => {
        // next (successObserver, failureObserver);
    };
}